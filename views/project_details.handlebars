<ul class="nav nav-tabs">
    <li>
        <a data-toggle="tab" href="#home">Project Overview</a>
    </li>
    <li>
        <a data-toggle="tab" href="#menu2">Project Activities</a>
    </li>
    <li>
        <a data-toggle="tab" href="#menu3">KMS Action Items</a>
    </li>
    <li>
        <a data-toggle="tab" href="#menu4">External Action Items</a>
    </li>
    <li>
        <a data-toggle="tab" href="#menu5">Cost Summary</a>
    </li>
    <li>
        <a data-toggle="tab" href="#menu6">PCO Log</a>
    </li>
    <li>
        <a data-toggle="tab" href="#menu7">Client Report</a>
    </li>
</ul>

<div class="tab-content">
    {{#ifEquals updateType 'updateProjectOverview'}}
    <div id="home" class="tab-pane fade in active"></div>
    {{/ifEquals}}
    {{#ifEquals updateType null}}
        <div id="home" class="tab-pane fade in active">
    {{else}}
    <div id="home" class="tab-pane fade in">
    {{/ifEquals}}
        <br><h3>Project Overview</h3>
        <table class="table table-striped ">
            <thead>
                <tr>
                    <th>Client Name</th>
                    <th>Project Name</th>
                    <th>Location</th>
                    <th>Target Start Date</th>
                    <th>Target Completion Date</th>
                    <th>Projected Budget</th>
                    <th>Service Type</th>
                </tr>
            </thead>
            <tbody>

                {{#each results}}
                <tr>
                    <td>{{clientName}}</td>
                    <td>{{projectName}}</td>
                    <td>{{location}}</td>
                    <td>{{formatDate targetStartDate}}</td>
                    <td>{{formatDate targetCompletionDate}}</td>
                    <td>${{projectedBudget}}</td>
                    <td>{{serviceType}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    {{#ifEquals updateType 'updateProjectActivity'}}
    <div id="menu2" class="tab-pane fade in active">
    {{else}}
    <div id="menu2" class="tab-pane fade in">
    {{/ifEquals}}
        <br><h3>Project Activities</h3>

    
   

        <table class="table table-striped ">
    <thead>
        <tr>
            
            <th>Activity Description</th>
            <th>Target Start Date</th>
            <th>Actual Start Date</th>
            <th>Target Completion Date</th>
            <th>Actual Completion Date</th>
            <th>Progress</th>
            <th>Activity Notes</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
           
        {{#each results1}}
        <form action="/projects/update" method="post">
        <tr >
            <input type="hidden" class="form-control" id="projectActivityID" name="projectActivityID" value="{{projectActivityID}}">
            <td><input type="text" class="form-control" name="activityDescription" value="{{activityDescription}}"> </td>
            <td><input type="date" class="form-control" name="targetStartDate" placeholder="{{formatDate targetStartDate}}" value="{{formatDate targetStartDate}}"></td>
            <td><input type="date" class="form-control" name="actualStartDate" placeholder="{{formatDate actualStartDate}}" value="{{formatDate actualStartDate}}" ></td>
            <td><input type="date" class="form-control" name="targetCompletionDate" placeholder="{{formatDate targetCompletionDate}}" value="{{formatDate targetCompletionDate}}" > </td>
            <td><input type="date" class="form-control" name="actualCompletionDate" placeholder="{{formatDate actualCompletionDate}}" value="{{formatDate actualCompletionDate}}"> </td>
            <td><input type="text" class="form-control" name="actualCompletionDate" placeholder="{{#if progress}}{{progress}}{{else}}0%{{/if}}" value="{{#if progress}}{{progress}}{{else}}0%{{/if}}"></td>
            <td><input type="text" class="form-control" namne="activityNotes" placeholder="{{activityNotes}}" value="{{activityNotes}}" > </td>
            <td><button type="submit" name="actionType" class="form-control" value="Update" formaction="/projects/updateProjectActivity">Update</button></td>
            <td><button type="submit" name="actionType" class="form-control" value="Delete" formaction="/projects/deleteProjectActivity">Delete</button></td>
        </tr>
        </form>
        {{/each}}
    </tbody>
</table>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Activity Description</th>
                    <th>Target Start Date</th>
                    <th>Actual Start Date</th>
                    <th>Target Completion Date</th>
                    <th>Actual Completion Date</th>
                    <th>Progress</th>
                    <th>Activity Notes</th>
                </tr>
            </thead>
            <tbody>

                {{#each results1}}
                <tr>
                    <td>{{activityDescription}}</td>
                    <td>{{formatDate targetStartDate}}</td>
                    <td>{{formatDate actualStartDate}}</td>
                    <td>{{formatDate targetCompletionDate}}</td>
                    <td>{{formatDate actualCompletionDate}}</td>
                    <td>{{#if progress}}{{progress}}{{else}}0%{{/if}}</td>
                    <td>{{activityNotes}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <div id="menu3" class="tab-pane fade">

        <br><h3>KMS Action Items</h3>
        <table class="table table-striped ">
            <thead>
                <tr>
                    <th>Action Item Description</th>
                    <th>Actvity Owner</th>
                    <th>Activity Start Date</th>
                    <th>Target Completion Date</th>
                    <th>Actual Completion Date</th>
                    <th>Action Item Notes</th>
                </tr>
            </thead>
            <tbody>

                {{#each results2}}
                <tr>
                    <td>{{actionItemDescription}}</td>
                    <td>{{activityOwner}}</td>
                    <td>{{formatDate activityStartDate}}</td>
                    <td>{{formatDate targetCompletionDate}}</td>
                    <td>{{formatDate actualCompletionDate}}</td>
                    <td>{{actualItemNotes}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

    </div>
    <div id="menu4" class="tab-pane fade">
        <br><h3>External Action Items</h3>
        <table class="table table-striped ">
            <thead>
                <tr>
                    <th>Action Item Description</th>
                    <th>Actvity Owner</th>
                    <th>Activity Start Date</th>
                    <th>Target Completion Date</th>
                    <th>Actual Completion Date</th>
                    <th>Action Item Notes</th>
                </tr>
            </thead>
            <tbody>

                {{#each results3}}
                <tr>
                    <td>{{actionItemDescription}}</td>
                    <td>{{activityOwner}}</td>
                    <td>{{formatDate activityStartDate}}</td>
                    <td>{{formatDate targetCompletionDate}}</td>
                    <td>{{formatDate actualCompletionDate}}</td>
                    <td>{{actualItemNotes}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <div id="menu5" class="tab-pane fade">
        <br><h3>Cost Summary</h3>
        <table class="table table-striped ">
            <thead>
                <tr>
                    <th>Division Category</th>
                    <th>Line Item</th>
                    <th>Line Item Totals</th>
                    <th>Budget</th>
                    <th>Committed</th>
                    <th>Change Orders</th>
                    <th>Anticipated Cost</th>
                    <th>Variance</th>
                    <th>Actual Cost To Date</th>
                    <th>Dollars Remaining</th>
                </tr>
            </thead>
            <tbody>

                {{#each results4}}
                <tr>
                    <td>{{divisionCategory}}</td>
                    <td>{{lineItem}}</td>
                    <td>{{lineItemTotals}}</td>
                    <td>${{budget}}</td>
                    <td>${{committed}}</td>
                    <td>{{changeOrders}}</td>
                    <td>${{anticipatedCost}}</td>
                    <td>${{variance}}</td>
                    <td>${{dollarsRemaining}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <div id="menu6" class="tab-pane fade">
        <br><h3>PCO Log</h3>
        <table class="table table-striped ">
            <thead>
                <tr>
                    <th>PCO Number</th>
                    <th>PCO Description</th>
                    <th>Due Date</th>
                    <th>To KMS Date</th>
                    <th>KMS Reviewed Date</th>
                    <th>Bank Approved Date</th>
                    <th>PCO Value</th>
                    <th>Contingency Amount</th>
                    <th>Cost Savings Amount</th>
                    <th>Change Orders</th>
                    <th>Status</th>
                    <th>Comments</th>
                </tr>
            </thead>
            <tbody>

                {{#each results5}}
                <tr>
                    <td>{{pcoNumber}}</td>
                    <td>{{pcoDescription}}</td>
                    <td>{{formatDate toKMSDate}}</td>
                    <td>{{formatDate kmsReviewDate}}</td>
                    <td>${{pcoValue}}</td>
                    <td>${{contingencyAmount}}</td>
                    <td>${{costSavingsAmount}}</td>
                    <td>{{changeOrders}}</td>
                    <td>{{status}}</td>
                    <td>{{comments}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <div id="menu7" class="tab-pane fade">

    </div>
</div>
</div>

<script src="handlebars/dist/handlebars.min.js"></script>
<script src="handlebars-intl/dist/handlebars-intl.min.js"></script>